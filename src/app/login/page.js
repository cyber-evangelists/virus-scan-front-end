import { LoginForm, LoginNav } from "@/components";
import React from "react";

const page = () => {
  return (
    <section>
      <LoginNav />
      <LoginForm />
    </section>
  );
};

export default page;
